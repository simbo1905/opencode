
bun and bunx none of that node junk.

## Diagrams

All agents mess up putting funny stuff into labels in mermaid. This is not UML! You MUST use autonumbering for all connectors, only have a able that is plain ascii no characters other than perhaps a <br/> and then build a table below that has number, the test of the lable, then the description where you are trying to stuff too much stuff onto the diagram. 

## Debugging

- To test opencode in the `packages/opencode` directory you can run `bun dev`

## SDK

To regenerate the javascript SDK, run ./packages/sdk/js/script/build.ts

## Tool Calling

- ALWAYS USE PARALLEL TOOLS WHEN APPLICABLE.
